{
  "todo" : [ 
    "build a simple example which demonstrates mouse picking and render order",
    "allow nodes to store a position in 2d space",
    "allow nodes to store arbitrary information which is used by the renderer",
    "do caching of bounding squares for clipping"
  ],
  "done" : [
    "build out a <i>very</i> minimal tree structure",
    "write unit tests to check that this tree will function as expected"
  ],
  "nope" : [
    "provide simple operations such as re-ordering nodes on a parent"
  ]
}

